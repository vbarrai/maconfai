const passthrough = (s: string) => s

export default {
  bgCyan: passthrough,
  black: passthrough,
  cyan: passthrough,
  green: passthrough,
  yellow: passthrough,
  red: passthrough,
  dim: passthrough,
}
